/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    trailingSlash: true,
    output: 'export',
    images: {
        domains: ['nguyenthanhluongtdc.github.io/finance']
    }
}
 module.exports = nextConfig