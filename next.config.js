/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    images: {
        domains: ['nguyenthanhluongtdc.github.io']
    },
    trailingSlash: true,
    output: 'export',
}
 module.exports = nextConfig